# chrome-mcp-bridge-2026-skill — 说明书

> **通过 Node.js 桥接脚本稳定连接 streamable-http MCP 服务，支持 `--server` 模式作为标准 MCP Server 供任意 AI 客户端使用。**

---

## 目录

- [这是什么](#这是什么)
- [能力概览](#能力概览)
- [快速安装](#快速安装)
- [使用方法](#使用方法)
  - [方式 A：MCP 客户端自动使用（推荐）](#方式-amcp-客户端自动使用推荐)
  - [方式 B：CLI 直接调用](#方式-bcli-直接调用)
- [CLI 命令参考](#cli-命令参考)
- [Session 管理](#session-管理)
- [项目结构](#项目结构)
- [技术细节](#技术细节)
- [已知限制](#已知限制)
- [相关资源](#相关资源)
- [许可证](#许可证)

---

## 这是什么

本技能包是一个 **MCP 桥接中间层**，解决一个特定的兼容性问题：

后端 MCP 服务（如 Chrome 浏览器自动化）使用 `streamable-http` 传输（HTTP POST + SSE，需要管理 session ID），但很多 AI 客户端只支持 `stdio` 传输。`mcp-bridge.js` 脚本作为中间层，自动管理 session 生命周期，让任何客户端都能用。

```
AI 客户端         stdin/stdout       mcp-bridge.js       HTTP POST+SSE       MCP 服务端
(Claude/Cursor/)  ◄─── MCP ─────►    (session 管理)      ◄─── 代理 ────►    (streamable-http)
                  (stdio 协议)                                                     │
                                                                          http://127.0.0.1:12306/mcp
```

## 能力概览

对接 [mcp-chrome-2026](https://github.com/phoenixlucky/mcp-chrome-2026) 服务，覆盖 **9 大类 30+ 浏览器自动化工具**：

| 分类 | 工具 | 核心能力 |
|:---:|:---|:---|
| 📊 浏览器管理 | `get_windows_and_tabs` · `chrome_navigate` · `chrome_close_tabs` · `chrome_switch_tab` | 窗口标签页管理、页面导航、历史控制 |
| 📸 截图视觉 | `chrome_screenshot` · `chrome_computer` · `chrome_gif_recorder` | 全页/元素截图、坐标鼠标键盘交互、操作 GIF 录制 |
| 🌐 网络监控 | `chrome_network_capture` · `chrome_network_request` · `chrome_block_images` | 请求捕获、自定义请求、资源拦截 |
| 🔍 内容分析 | `chrome_read_page` · `chrome_get_web_content` · `chrome_get_page_text` · `chrome_extract` | 无障碍树分析、内容提取、Readability 正文解析、结构化数据抽取 |
| 🎯 交互操作 | `chrome_click_element` · `chrome_fill_or_select` · `chrome_keyboard` · `chrome_upload_file` · `chrome_handle_dialog` · `chrome_request_element_selection` | 点击、表单填写、键盘快捷键、文件上传、对话框处理、人工辅助选元素 |
| 💻 脚本执行 | `chrome_javascript` · `chrome_console` | 在页面上下文执行 JS、捕获控制台日志 |
| 📚 数据管理 | `chrome_history` · `chrome_bookmark_search/add/delete` | 历史记录检索、书签 CRUD |
| 🕸️ 抓取提取 | `chrome_get_tab_url` · `chrome_scroll` · `chrome_get_scroll_state` · `chrome_wait` · `chrome_click_and_wait` | URL 获取、滚动控制、等待元素、组合操作 |
| ⚡ 性能分析 | `performance_start_trace` · `performance_stop_trace` · `performance_analyze_insight` | 页面性能 Trace 录制、DevTools 性能分析 |

> 执行 `node mcp-bridge.js call tools/list` 可获取实时的工具列表及参数签名。

## 快速安装

### 前置条件

| 要求 | 说明 |
|------|------|
| **Node.js** ≥ 18 | 内置 `fetch` API，无需额外依赖 |
| **MCP 服务端** | 运行在 `http://127.0.0.1:12306/mcp` 的 streamable-http 服务 |

### 1️⃣ 安装 Chrome MCP 服务

本技能包需要一个运行中的 [mcp-chrome-2026](https://github.com/phoenixlucky/mcp-chrome-2026) 服务作为后端：

```bash
# 安装 Native Host 桥接器（npm）
npm install -g @ethanwilkins/mcp-chrome-bridge-2026

# 启动服务
mcp-chrome-bridge start
```

服务将在 `http://127.0.0.1:12306/mcp` 监听。确认在线：

```bash
curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:12306/mcp
# 预期输出: 200
```

### 2️⃣ 获取本技能包

```bash
git clone https://github.com/phoenixlucky/chrome-mcp-bridge-2026-skill.git
cd chrome-mcp-bridge-2026-skill
```

## 使用方法

### 方式 A：MCP 客户端自动使用（推荐）

通过 `.mcp.json` 将桥接脚本注册为 stdio MCP Server，启动客户端后 `chrome` 工具会自动可用。

**单文件配置**（在项目根目录创建 `.mcp.json`）：

```json
{
  "mcpServers": {
    "chrome": {
      "command": "node",
      "args": ["<脚本绝对路径>\\mcp-bridge.js", "--server"],
      "env": {
        "MCP_SERVER_URL": "http://127.0.0.1:12306/mcp"
      }
    }
  }
}
```

也可以直接在 Reasonix 中使用——本项目已在根目录提供 `.mcp.json`，安装后自动生效。

### 方式 B：CLI 直接调用

适合 shell 环境和调试场景。一条命令完成初始化 + 调用：

```powershell
# 初始化连接
node mcp-bridge.js init

# 列出可用工具
node mcp-bridge.js call tools/list

# 调用工具（推荐 --stdin 避免 PowerShell 转义）
$body = @'
{"name":"chrome_navigate","arguments":{"url":"https://example.com"}}
'@
$body | node mcp-bridge.js call tools/call --stdin

# 心跳保活
node mcp-bridge.js ping

# 关闭连接
node mcp-bridge.js close
```

## CLI 命令参考

| 命令 | 参数 | 说明 |
|:---|:---|:---|
| `init` | — | 初始化 MCP 连接，获取 session ID |
| `call` | `<method>` `[params\|--stdin]` | 调用 JSON-RPC 方法 |
| `ping` | — | 心跳保活，延长 session 有效期 |
| `close` | — | 发送 close 通知，清理 session 文件 |
| `path` | — | 输出脚本自身绝对路径 |
| *(无参数)* | — | 显示帮助信息 |

**参数传递方式：**

```powershell
# 方式一：命令行直接传入（简单参数）
node mcp-bridge.js call tools/call '{"name":"chrome_navigate","arguments":{"url":"https://example.com"}}'

# 方式二：--stdin 管道模式（推荐，避免 & 等特殊字符被截断）
echo '{"name":"chrome_navigate","arguments":{"url":"https://example.com"}}' | node mcp-bridge.js call tools/call --stdin

# 从文件重定向
node mcp-bridge.js call tools/call --stdin < params.json
```

## Session 管理

```
启动 → init → POST /mcp {initialize}
                ↓
         服务端分配 sessionId
                ↓
         持久化到 %TEMP%/mcp-bridge-session.json
                ↓
         调用/心跳/关闭 ← 自动恢复机制（超时→清理→re-init）
```

- **Session ID** 保存在系统临时目录（`%TEMP%/mcp-bridge-session.json`）
- 每次请求自动附加 `Mcp-Session-Id` 请求头
- Session 超时后 **自动检测、清理并重建**，无需人工干预
- `--server` 模式下全程自动管理

## 项目结构

```
chrome-mcp-bridge-2026-skill/
├── mcp-bridge.js          # 🧩 Node.js 桥接脚本（核心）
├── SKILL.md               # 📘 AI 代理操作手册
├── .mcp.json              # ⚙️ MCP 配置（注册为 stdio Server）
├── README.md              # 📖 本文件（项目说明书）
├── README.md              # 桥接脚本文档
├── reasonix.toml          # ⚙️ Reasonix 项目配置
├── .gitignore             # 🔒 版本控制忽略规则
└── LICENSE                # ⚖️ MIT 许可证
```

## 技术细节

### MCP Streamable HTTP Transport

| 阶段 | 请求头 | 响应处理 |
|:---|:---|:---|
| 初始化 | `Accept: text/event-stream, application/json` | 提取 `Mcp-Session-Id` 响应头 |
| 调用 | `Mcp-Session-Id: <id>` + 同上 Accept | 解析 JSON 或 SSE 流 |
| 通知 | 同上 | 无需等待响应 |

- 零外部依赖：仅使用 Node.js v18+ 内置 `fetch` API
- 双协议解析：即时 JSON 响应和 SSE 流式响应均正确解析
- `--server` 模式可作为标准 MCP Server 供任意客户端连接

### Chrome MCP 服务

基于 Chrome DevTools Protocol (CDP) 提供全面的浏览器自动化能力。所有工具返回统一格式：

```json
{
  "content": [{ "type": "text", "text": "响应数据..." }],
  "isError": false
}
```

## 已知限制

1. **SPA 动态页面内容提取不全**：`chrome_get_page_text` 使用 Readability 提取，不适合 X/Twitter 等 SPA。改用 `chrome_extract` 配合 CSS 选择器或 `chrome_get_web_content`。

2. **全页截图失败**：某些页面下 `fullPage: true` 会报错 `image readback failed`。改用普通截图。

3. **滚动懒加载**：某些 SPA 使用虚拟列表，滚动高度增加但不加载新内容。结合多次小步滚动 + `chrome_extract` 分段提取。

## 相关资源

| 资源 | 链接 |
|:---|:---|
| 📘 MCP 规范文档 | [spec.modelcontextprotocol.io](https://spec.modelcontextprotocol.io) |
| 🌐 Chrome MCP 服务 | [github.com/phoenixlucky/mcp-chrome-2026](https://github.com/phoenixlucky/mcp-chrome-2026) |
| 🤖 Reasonix 框架 | [reasonix.ai](https://reasonix.ai) |
| 📄 Chrome MCP 工具文档 | [TOOLS_zh.md](https://github.com/phoenixlucky/mcp-chrome-2026/blob/master/docs/TOOLS_zh.md) |
| 🔧 桥接脚本详细用法 | [README.md](./README.md) |
| 🤖 AI 代理操作手册 | [SKILL.md](./SKILL.md) |

## 许可证

[MIT](LICENSE) © 2026 [phoenixlucky](https://github.com/phoenixlucky)

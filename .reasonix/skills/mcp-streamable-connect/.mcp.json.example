{
  "mcpServers": {
    "chrome-bridge": {
      "description": "Chrome 浏览器自动化（通过 mcp-chrome-2026 桥接）",
      "command": "node",
      "args": [
        "${BRIDGE_PATH}",
        "--server"
      ],
      "env": {
        "MCP_SERVER_URL": "http://127.0.0.1:12306/mcp"
      }
    }
  }
}
